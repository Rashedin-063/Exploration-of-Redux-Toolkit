const numbers = [1, 3, 5, 8, 10];

const index = numbers.findIndex(num => num > 4)

console.log(index)


// indexOf works for primitive values
// findIndexOf works for non-primitive values and with a callback function
